# NextJS - Good (Hosted on Vercel) - NOTES, not yet executed

Previous tier: [NextJS - Bare Minimum](../NextJS%20Github%20Repository/0003-NextJS-Bare-Minimum.md)

This tier hasn't been run yet. Once it's executed, its contents graduate into a numbered lifecycle doc and this note gets deleted

## The level up from Bare Minimum

Bare Minimum bootstraps the app with `create-next-app` defaults. Good/Better/Best should include how I'd like to configure eslint, typescript-eslint, prettier, etc. I'll start with "Bare-minimum" myself and go from there
